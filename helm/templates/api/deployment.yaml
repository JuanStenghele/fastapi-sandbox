apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-sandbox-deployment
spec:
  replicas: {{ .Values.replicaCount.api }}
  selector:
    matchLabels:
      app: fastapi-sandbox
  template:
    metadata:
      labels:
        app: fastapi-sandbox
    spec:
      containers:
      - name: fastapi-sandbox
        image: {{ .Values.image.api.repository }}:{{ .Values.image.api.tag }}
        imagePullPolicy: {{ .Values.image.api.pullPolicy }}
        envFrom:
        - secretRef:
            name: fastapi-sandbox-secrets
        - configMapRef:
            name: fastapi-sandbox-config-map
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        ports:
        - containerPort: 8000
